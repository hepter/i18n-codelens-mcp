import { t } from 'i18next';

const home = t("common:nav.home");
const about = t("common:nav.about");
const login = t("auth:login.title");
const forgot = t("auth:login.forgot");
const unused = t("common:btn.save");
